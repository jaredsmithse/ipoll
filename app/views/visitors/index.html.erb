<h3>Welcome</h3>
<p><%= link_to 'Users:', users_path %> <%= User.count %> registered</p>
<% if user_signed_in? %>
    <h3>Room Name:</h3>
    <%= simple_form_for @room do |f| %>
  	<%= f.input :name %>
  	<%= f.button :submit %>
	<% end %>
<% end %>